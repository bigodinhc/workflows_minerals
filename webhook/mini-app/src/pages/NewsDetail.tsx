export default function NewsDetail({
  itemId: _,
  onBack: __,
}: {
  itemId: string;
  onBack: () => void;
}) {
  return (
    <div className="p-4 text-text-secondary">News detail loading...</div>
  );
}
